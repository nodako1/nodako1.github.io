# PokeDeck Public Repository
公開用(main)には以下を含みます:
- admin-ui / ios / admin-api / public-api
※秘密情報や生成物（node_modules, dist, *.env, サービスアカウントJSON）は含めません。
管理UI設定は admin-ui/config.js の adminApiBaseUrl を参照。
